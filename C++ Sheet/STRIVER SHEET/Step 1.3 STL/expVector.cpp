#include<bits/stdc++.h>
using namespace std;

void explainVector() {

    vector<int> V;

    V.push_back(1);
    V.emplace_back(2);

    vector<pair<int, int>>vec;

    V.push_back({1 , 2});
    V.emplace_back(1 , 2);

    vector<int> v(5 , 100);

    vector<int> v(5);

    vector<int> v1(5 , 20);
    vector<int> v2(v1);

}